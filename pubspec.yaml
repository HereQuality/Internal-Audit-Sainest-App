name: internal_audit_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.1.1+6

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  dio: ^5.7.0
  provider: ^6.1.2
  flutter_secure_storage: ^9.2.2
  socket_io_client: ^2.0.3+1
  image_picker: ^1.1.2
  intl: ^0.19.0
  cached_network_image: ^3.4.1
  timeago: ^3.7.0

  # ── Notifications (see lib/core/notifications) — local polling (event_
  # poll.dart/overdue_poll.dart, works even with no Firebase project set
  # up) AND real FCM push (fcm_service.dart, arrives with the app fully
  # closed/killed — needs android/app/google-services.json + iOS's
  # GoogleService-Info.plist to actually deliver; degrades to "local-only"
  # everywhere else in this file until those exist, see fcm_service.dart's
  # own doc comment) ──
  flutter_local_notifications: ^18.0.1
  flutter_background_service: ^5.1.0
  permission_handler: ^11.3.1
  timezone: ^0.9.4
  flutter_timezone: ^5.1.0
  shared_preferences: ^2.3.3
  http: ^1.2.2
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.6

  # Reports screen (profile) — builds and shares a completed audit's full
  # report as a PDF, mirroring client/src/pages/AuditFullReport.jsx.
  pdf: ^3.11.1
  printing: ^5.13.4
  # Was already a transitive dependency of pdf (which uses it to decode
  # any image handed to pw.MemoryImage) — declared directly so
  # report_pdf_builder.dart can decode-and-validate a fetched image ITSELF
  # before ever constructing a pw.MemoryImage from it. See that file's own
  # note on why: pw.MemoryImage defers decoding until the document is
  # actually rendered, so an image this package can't decode (e.g. a
  # lossless-VP8L WebP it has a decoder bug on) throws from deep inside
  # doc.save() instead of at fetch time, uncatchable by the fetch's own
  # try/catch and surfacing as a whole report failing to generate.
  image: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates android/app/src/main/res/mipmap-*/ic_launcher.png and
  # ios/Runner/Assets.xcassets/AppIcon.appiconset/*.png from
  # assets/icon/app_icon.png (same source as the web app's favicon, see
  # client/src/assets/Fevicon_hqepl.png). Run: dart run
  # flutter_launcher_icons
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  # Flutter's default template icon shows through as a translucent square
  # on some Android launchers if the source has transparency and no
  # background is set for the adaptive icon layer — white matches the
  # icon's own background so it looks the same as a normal square icon.
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/app_icon.png"
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/images/
    - assets/icon/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
